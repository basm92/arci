# Plot method for ar_ci objects

Plot method for ar_ci objects

## Usage

``` r
# S3 method for class 'ar_ci'
plot(x, ...)
```

## Arguments

- x:

  An object of class 'ar_ci' returned by the `ar_ci` function.

- ...:

  Additional arguments passed to the plot function.
